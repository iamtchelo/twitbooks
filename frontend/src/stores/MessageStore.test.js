const NOOP_CLIENT = {
    getMessages: () => {
        return Promise.resolve({
        })
    }
};

test("test set page should normalize the input for the API", () => {
});

test("get messages should update the store", async () => {
});